{
  "name": "education-system",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch-server": "cd server && npm run dev",
    "watch-client": "cd client && npm run dev -- --host",
    "dev": "concurrently -c auto --handle-input npm:watch-*"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.6.0"
  }
}
