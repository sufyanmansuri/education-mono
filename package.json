{
  "name": "education-system",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch-client": "cd client && npm run dev -- --host",
    "watch-server": "cd server && npm run dev",
    "dev": "concurrently -c auto npm:watch-*"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "client",
    "server"
  ],
  "private": true,
  "dependencies": {
    "concurrently": "^7.6.0"
  }
}
